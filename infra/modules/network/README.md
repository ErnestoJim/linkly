# network

VPC, subnets (public/private), NAT gateways, route tables and security
groups shared by the other modules (`eks`, `rds`, `sqs`).

Not implemented yet — placeholder for the networking module.
