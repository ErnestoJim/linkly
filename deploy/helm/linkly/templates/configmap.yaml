apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "linkly.fullname" . }}-env
  labels:
    {{- include "linkly.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
