apiVersion: v1
kind: Service
metadata:
  name: {{ include "linkly.fullname" . }}-api
spec:
  selector:
    app: {{ include "linkly.fullname" . }}-api
  ports:
    - port: 80
      targetPort: {{ .Values.api.port }}
