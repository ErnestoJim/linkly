apiVersion: v1
kind: Service
metadata:
  name: {{ include "linkly.api.fullname" . }}
  labels:
    {{- include "linkly.api.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "linkly.api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
